import styled from 'styled-components';

export const StyledCartContainer = styled.div`
  background: #ffffff;
  border-radius: 12px;
  padding: 1.5rem;
  font-family: var(--bs-font-sans-serif);
`;

export const PriceItemRow = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.5rem 0;
`;